{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <button class="btn btn-primary">{{ button_label|default('app.actions.save'|trans) }}</button>
    </div>
{{ form_end(form) }}
